# Hugging Face Space 用。CPU Space / ZeroGPU の両方で動作する。
# （ローカルCPU環境で軽量に入れたい場合: pip install torch --index-url https://download.pytorch.org/whl/cpu）
torch>=2.4
torchvision>=0.19

Pillow>=10.0
numpy

open_clip_torch>=2.32   # SigLIP 2 (ViT-B-16-SigLIP2-256, WebLI)
transformers>=4.57      # DINOv2 (facebook/dinov2-small) / PixAI Tagger v1.0 (custom code)
timm                    # PixAI Tagger v1.0 の依存
dreamsim>=0.2.1         # DreamSim ensemble (知覚的類似度)
dghs-imgutils>=0.15     # CCIP + WD14 tagger v3
onnxruntime>=1.16       # imgutils の ONNX 推論バックエンド
huggingface_hub
spaces                  # ZeroGPU 対応（CPU Space では no-op）
